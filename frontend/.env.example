# Backend API base URL (no trailing slash). Used by app/api/[...path] proxy.
# Local:
BACKEND_API_URL=http://127.0.0.1:8001

# Production (Vercel) — set in Vercel Project → Settings → Environment Variables:
# BACKEND_API_URL=https://your-api.onrender.com
